export const API_ENDPOINTS = {
  SUBJECTS: '/api/subjects',
  EXAMS: '/api/exams',
  TOPICS: '/api/topics',
} as const;

export const QUERY_KEYS = {
  SUBJECTS: ['subjects'],
  EXAMS: ['exams'],
  TOPICS: ['topics'],
} as const;
